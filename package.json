{
  "name": "procesamiento",
  "description": "Módulo de Procesamiento",
  "version": "0.1.2",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "license": "MIT",
  "main": "server/app-procesamiento.js",
  "types": "server/app-procesamiento.d.ts",
  "files": [
    "client",
    "install",
    "server",
    "unlogged"
  ],
  "dependencies": {
    "operativos": "~0.1.14",
    "datos-ext": "~0.1.6",
    "varcal": "~0.1.21",
    "pg-triggers": "^0.2.1"
  },
  "devDependencies": {
    "@types/node": "~10.3.4",
    "@types/express": "~4.16.0",
    "pg-promise-strict": "~0.4.9"
  },
  "engines": {
    "node": ">= 8.0.0"
  },
  "scripts": {
    "x-test": "tsc || echo \"continue with errors\" && mocha --reporter spec --bail --check-leaks tests\\tests\\*.js",
    "test": "(npm run prepublish || echo \"continue with errors\") && mocha --reporter spec --bail --check-leaks tests/tests/*.js",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks tests/tests/",
    "start": "node server/server-procesamiento.js",
    "prepublish": "(tsc -p tsconfig-client.json || echo \"continue w/error\") && (tsc -p tsconfig-server.json  || echo \"continue w/error\")"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "lib",
    "ecmaVersion": 6
  }
}
